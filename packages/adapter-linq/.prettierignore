# Generated by changesets on every release. It renders a multi-paragraph
# changeset with an indented blank line, which oxfmt rejects as trailing
# whitespace — a release PR should not fail formatting on a file no one edits.
CHANGELOG.md
